# Автоматическое появление Tooltip

## ✅ Выполнено: 17 января 2026

### Задача
Добавить автоматическое появление tooltip на 10 секунд после завершения печатания и изменить текст

### Реализация

#### 1. Автоматический показ tooltip
```tsx
const [showTooltipAuto, setShowTooltipAuto] = useState(false);

// Автоматически показать tooltip на 10 секунд
useEffect(() => {
  if (highlightApplied && !showTooltipAuto) {
    const showTimer = setTimeout(() => {
      setShowTooltipAuto(true);
      
      // Скрыть через 10 секунд
      const hideTimer = setTimeout(() => {
        setShowTooltipAuto(false);
      }, 10000);
      
      return () => clearTimeout(hideTimer);
    }, 1000);
    
    return () => clearTimeout(showTimer);
  }
}, [highlightApplied, showTooltipAuto]);
```

#### 2. Применение класса к tooltip
```tsx
<span className={`tooltip ${showTooltipAuto ? 'tooltip-auto-show' : ''}`}>
  <span className="tooltip-icon">📁</span>
  <span className="tooltip-text">
    посмотреть примеры на Google Drive
  </span>
  <svg className="arrow-icon">...</svg>
</span>
```

#### 3. CSS для автопоказа
```css
.tooltip.tooltip-auto-show {
  opacity: 1 !important;
  visibility: visible !important;
  pointer-events: auto !important;
  transform: translateX(-50%) translateY(0) !important;
  
  /* Анимация привлечения внимания */
  animation: tooltipAutoAppear 0.5s ease-out, tooltipPulse 2s ease-in-out 0.5s 2;
}

@keyframes tooltipAutoAppear {
  0% {
    opacity: 0;
    transform: translateX(-50%) translateY(10px) scale(0.9);
  }
  60% {
    transform: translateX(-50%) translateY(0) scale(1.05);
  }
  100% {
    opacity: 1;
    transform: translateX(-50%) translateY(0) scale(1);
  }
}

@keyframes tooltipPulse {
  0%, 100% {
    transform: translateX(-50%) translateY(0) scale(1);
  }
  50% {
    transform: translateX(-50%) translateY(0) scale(1.03);
  }
}
```

### Сценарий работы

1. **Печатание (0-5 сек)**: Весь текст печатается
2. **Стилизация (5 сек)**: "более 200 AI-видео" становится золотым
3. **Автопоказ (через 1 сек = 6 сек)**: Tooltip появляется автоматически
4. **Показ (6-16 сек)**: Tooltip видим 10 секунд с лёгкой пульсацией
5. **Автоскрытие (16 сек)**: Tooltip плавно исчезает
6. **Hover (всегда)**: При наведении tooltip появляется снова

### Особенности

1. **Задержка**: 1 секунда после применения подсветки
2. **Длительность**: 10 секунд показа
3. **Анимация**: Плавное появление с увеличением
4. **Пульсация**: 2 раза пульсирует для привлечения внимания
5. **Текст**: "посмотреть примеры на Google Drive"
6. **Hover**: Продолжает работать после автопоказа

### Параметры

- **autoShowDelay**: 1000ms (1 секунда)
- **autoShowDuration**: 10000ms (10 секунд)
- **Пульсация**: 2 раза по 2 секунды каждая

### Результат

✅ Tooltip появляется автоматически через 1 секунду  
✅ Показывается ровно 10 секунд  
✅ Плавно исчезает  
✅ При hover продолжает работать  
✅ Текст: "посмотреть примеры на Google Drive"  
✅ Лёгкая анимация привлекает внимание  
✅ Профессиональный вид

### Файлы изменены
- `ai-portfolio/app/components/Cases.tsx` - добавлен автопоказ tooltip
- `ai-portfolio/app/globals.css` - добавлены анимации для автопоказа
